@model List<CartItemViewModel>
@{
    ViewBag.Title = "Sepetim";
}
<div class="container mt-5">
    <h2>Sepetim</h2>
    @if (Model.Count == 0)
    {
        <p>Sepetiniz boş.</p>
    }
    else
    {
        <table class="table">
            <thead>
            <tr>
                <th>Kurs Adı</th>
                <th>Fiyat</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.CourseName</td>
                    <td>@item.Price.ToString("C")</td>
                    <td>
                        <!-- Kaldırma işlemi için buton ekleyebilirsiniz -->
                    </td>
                </tr>
            }
            </tbody>
        </table>
        <div class="mt-3">
            <a href="@Url.Action("Checkout", "Order")" class="btn btn-primary">Satın Al</a>
        </div>
    }
</div>